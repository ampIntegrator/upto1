/**
 * The modal slot on any other address: empty. Without it, a client-side navigation from an open
 * modal to a page (a footer button, a form's redirect) would keep the modal on screen.
 */
export default function ModalSlotElsewhere() {
  return null;
}
