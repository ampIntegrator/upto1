/**
 * The modal slot of the site layout: empty unless a client-side link opens a modal
 * (@modal/(.)modale/[slug]). default.tsx covers full page loads (docs/modals.md).
 */
export default function ModalSlotDefault() {
  return null;
}
