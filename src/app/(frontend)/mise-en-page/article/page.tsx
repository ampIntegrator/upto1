import React from 'react';

import {PostDemo} from '../PostDemo';

export const metadata = {title: 'Mise en page — article de blog (maquette 18)'};

export default function Page() {
  return <PostDemo />;
}
