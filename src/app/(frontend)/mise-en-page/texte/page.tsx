import React from 'react';

import {PageDemo} from '../PageDemo';

export const metadata = {title: 'Mise en page — plein écran · texte et image (maquette 02)'};

export default function Page() {
  return <PageDemo hero="split" blocks="dark" />;
}
