import React from 'react';

import {PageDemo} from '../PageDemo';

export const metadata = {title: 'Mise en page — hero texte plein écran (maquette 02)'};

export default function Page() {
  return <PageDemo hero="split" />;
}
