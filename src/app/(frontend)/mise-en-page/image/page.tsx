import React from 'react';

import {PageDemo} from '../PageDemo';

export const metadata = {title: 'Mise en page — haut de page · image (maquette 25 A)'};

export default function Page() {
  return <PageDemo hero="page-image" />;
}
