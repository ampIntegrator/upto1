import React from 'react';

import {CaseDemo} from '../CaseDemo';

export const metadata = {title: 'Mise en page — réalisation (maquette 23)'};

export default function Page() {
  return <CaseDemo />;
}
