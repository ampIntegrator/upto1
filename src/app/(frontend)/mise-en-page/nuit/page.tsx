import React from 'react';

import {PageDemo} from '../PageDemo';

export const metadata = {title: 'Mise en page — haut de page · nuit (maquette 25 C)'};

export default function Page() {
  return <PageDemo hero="page-night" />;
}
